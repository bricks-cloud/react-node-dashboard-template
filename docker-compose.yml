version: '3.7'

services:
  my-app-dev:
    container_name: bricks-dashboard-dev
    build:
      context: .
      dockerfile: Dockerfile
    volumes:
      - '.:/app'
      - '/app/node_modules'
    ports:
      - 8080:8080

