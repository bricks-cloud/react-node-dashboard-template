# build environment
FROM node:16 as build
WORKDIR /app
ENV PATH /app/node_modules/.bin:$PATH
COPY package.json ./
COPY yarn.lock .
RUN yarn install
COPY . ./
EXPOSE 3000
CMD ["yarn", "run", "start-server"]